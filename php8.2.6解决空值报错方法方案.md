php8.2.6

解决变量空值报错方法方案


$a3 =$_POST['a'];
if (!is_null($a3)) {
    //可执行代码块
//嵌套代码可解决变量空值报错，方案


 }

解决变量空值报错方法

$t=$_POST['c'];  

if ($t>0)
{
   
//可执行代码块
//嵌套代码可解决变量空值报错，方案

}
else

{
    
    echo "";
   
    
}

























